export { COOKIE_NAME, ONE_YEAR_MS } from "@shared/const";

// Simple login function for prototype
export const startLogin = () => {
  // For prototype, we'll use a simple mock login
  console.log("Login initiated");
};
